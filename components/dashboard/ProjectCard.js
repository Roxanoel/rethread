import { StyleSheet, View, Text } from "react-native";

function ProjectCard({ name }) {
    return (
        <View>
            <Text>{name}</Text>
        </View>
    )
}

export default ProjectCard;